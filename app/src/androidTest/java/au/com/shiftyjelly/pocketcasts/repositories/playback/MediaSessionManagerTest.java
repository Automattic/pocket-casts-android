package au.com.shiftyjelly.pocketcasts.repositories.playback;

import org.junit.Test;

import static org.junit.Assert.assertArrayEquals;
import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNotNull;

// TODO uncomment or remove this test
public class MediaSessionManagerTest {

    @Test
    public void test() throws Exception {

    }

    // TODO bring this back
//    @Test
//    public void testCalculateSearchQueryAttempts() throws Exception {
//
//        List<String> options = MediaSessionManagerImpl.calculateSearchQueryOptions("Daily tech news in");
//        assertNotNull(options);
//        assertEquals(options.toString(), options.size(), 4);
//        assertArrayEquals(options.toArray(new String[0]), new String[] { "Daily tech news in", "Daily tech news", "Daily tech", "Daily" });
//
//        options = MediaSessionManagerImpl.calculateSearchQueryOptions("Material");
//        assertNotNull(options);
//        assertEquals(options.toString(), options.size(), 1);
//        assertArrayEquals(options.toArray(new String[0]), new String[] { "Material" });
//    }
}